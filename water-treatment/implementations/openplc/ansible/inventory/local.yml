# Local inventory for Phase 1 testing
# Deploys OpenPLC containers on the local machine

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

  children:
    plc_hosts:
      hosts:
        localhost:
